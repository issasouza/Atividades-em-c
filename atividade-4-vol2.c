#include "stdio.h"
#include <math.h>

int main(void)
{
int TDiarias;
float TaxaA, TaxaB, TaxaC, diarias = 50.00, Total;
char nome[30];
TaxaA = 10.00 ;
TaxaB = 15.30;
TaxaC = 8.50;
Total = diarias * TDiarias;

printf("\nDigite seu nome: ");
 
scanf("%s", &nome);
 
printf("\nDigite quantos dias você vai ficar no hotel: ");
scanf("%d",&TDiarias);
 
if (TDiarias<15);
 
    TaxaB;
 

if (TDiarias=15)

TaxaA ;
 
 
if (TDiarias >15)

TaxaC ;

Total = diarias * TDiarias;
 
printf("Você ficou no hotel %d  Dias, \nO total da sua estadia ficou: %.2f Reais,\n E a taxa foi de %.2f Reais", TDiarias, Total,TaxaA,TaxaB,TaxaC);
 

return 0;
 
  }
